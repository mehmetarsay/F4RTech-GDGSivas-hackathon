enum UserType { VOLUNTEER, SHARER }
//Volunteer => GÖNÜLLÜ => Ürünleri ulaştıran.
//Sharer => HAYIRSEVER => Ürünleri temin eden.

enum ProductType { FOOD, CLOTHES }

enum RequestStatus { WAITING, COMPLETED, DENIED }

enum ViewState { Idle, Busy }

enum ProductViewState { Idle, Loaded, Busy }

enum RequestViewState { Idle, Loaded, Busy }
